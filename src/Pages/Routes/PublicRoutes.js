import React from 'react';
import { Routes } from 'react-router-dom';

const PublicRoutes = () => {
    return (
        <div>
            
            <Routes>

            </Routes>
        </div>
    );
};

export default PublicRoutes;